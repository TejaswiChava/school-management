(function () {
    'use strict';


angular.module('sms').controller('contactCtrl', contactcontroller);

contactCtrl.$inject = ['UserService', '$location', '$rootScope', 'FlashService'];

function contactcontroller(UserService, $location, $rootScope, FlashService) {


    }

})(); 